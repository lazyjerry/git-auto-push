# Git 自動化工具配置文件
# ================================
# 
# 使用方式：
#   1. 複製此文件為 .env（移除 .example 後綴）
#   2. 根據需求修改設定值
#   3. 腳本會自動讀取配置
#
# 配置文件讀取優先級（由高到低）：
#   1. 當前工作目錄：$PWD/.git-auto-push-config/.env
#   2. 用戶 Home 目錄：$HOME/.git-auto-push-config/.env
#   3. 腳本所在目錄：[script_dir]/.git-auto-push-config/.env
#
# 注意：
#   - 只需設定要覆蓋的選項，未設定的選項會使用預設值
#   - 配置值請使用 Bash 語法
#   - 以 # 開頭的行為註解

# ==============================================
# 通用設定（git-auto-push.sh 和 git-auto-pr.sh 共用）
# ==============================================

# AI 工具優先順序（依陣列順序調用，失敗時自動嘗試下一個）
# 可用工具：copilot, gemini, codex, claude
# 範例：AI_TOOLS=("copilot" "gemini" "codex" "claude")
# AI_TOOLS=("copilot" "gemini" "codex" "claude")

# 調試模式（true=顯示詳細調試訊息，false=簡潔輸出）
# IS_DEBUG=false

# ==============================================
# git-auto-push.sh 專用設定
# ==============================================

# 任務編號自動帶入（true=自動加入 [任務編號] 前綴，false=不加）
# 任務編號從分支名稱偵測，如：JIRA-123、PROJ-456、feat-001
# AUTO_INCLUDE_TICKET=true

# Commit 訊息品質檢查（true=自動檢查，false=詢問模式）
# 使用 AI 檢查訊息是否具有明確的目的和功能性
# AUTO_CHECK_COMMIT_QUALITY=true

# AI Commit 訊息生成提示詞（自訂 AI 生成 commit 訊息的指令）
# AI_COMMIT_PROMPT="根據以下 git 變更生成一行中文 commit 標題，格式如：新增用戶登入功能、修正檔案上傳錯誤、改善搜尋效能。只輸出標題："

# AI 前綴選擇提示詞（自訂 AI 選擇 Conventional Commits 前綴的指令）
# AI_PREFIX_PROMPT="根據以下 git 變更，選擇最適合的 Conventional Commits 前綴類型..."

# ==============================================
# git-auto-pr.sh 專用設定
# ==============================================

# 主分支候選清單（依陣列順序檢測第一個存在的遠端分支）
# DEFAULT_MAIN_BRANCHES=("uat" "main" "master")

# 預設使用者名稱（用於生成分支名稱前綴：username/type/issue-description）
# DEFAULT_USERNAME="jerry"

# PR 合併後分支刪除策略（true=自動刪除，false=保留分支）
# AUTO_DELETE_BRANCH_AFTER_MERGE=false

# ==============================================
# 進階設定（一般情況下不需要修改）
# ==============================================

# Conventional Commits 前綴類型清單（格式："前綴:說明"）
# COMMIT_PREFIXES=(
#     "feat:新功能"
#     "fix:錯誤修復"
#     "docs:文件變更"
#     "style:程式碼格式"
#     "refactor:重構"
#     "perf:效能改進"
#     "test:測試相關"
#     "build:建置系統"
#     "ci:CI 配置"
#     "chore:雜項維護"
#     "revert:回退提交"
# )
